.zig-cache/
zig-cache/
zig-out/
result
result-*
.direnv/
.DS_Store
*.swp
*.swo

# Vault-symlinked personal references (not project assets — kept locally only)
ZIG_RECENT_API_CHANGES.md
ZIG_0.15_TO_0.16_MIGRATION.md
jj_cheatsheet.md

# Cross-project agent inbox — transient incoming messages from sibling
# projects' LLMs (per CLAUDE.md convention). Local-only working state.
inbox/

# Non-committed scratch space: ad-hoc analysis scripts, manual test
# harnesses against private corpora, etc. Anything in here is local-only.
scratch/

# Claude Code per-session state (lock files, scheduled task records).
.claude/
